<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
  
  <!-- ====== PWA META ====== -->
  <link rel="manifest" href="manifest.json">
  <meta name="theme-color" content="#0e0e12">
  <meta name="apple-mobile-web-app-capable" content="yes">
  <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
  <link rel="apple-touch-icon" href="icon-192.png">
  <link rel="icon" type="image/png" sizes="192x192" href="icon-192.png">
  <link rel="icon" type="image/png" sizes="512x512" href="icon-512.png">
  
  <!-- ====== SEO ====== -->
  <title>Kagami · Baca Manga & Nonton Anime</title>
  <meta name="description" content="Baca manga, manhwa, manhua dan nonton anime online gratis.">
  
  <!-- ====== FONT AWESOME ====== -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
  
  <style>
    * { margin: 0; padding: 0; box-sizing: border-box; font-family: 'Segoe UI', sans-serif; }
    body { background: #0e0e12; color: #e5e5eb; padding: 0 1.5rem 3rem; min-height: 100vh; }
    
    /* Install Banner */
    #installBanner {
      display: none;
      position: fixed;
      bottom: 0;
      left: 0;
      right: 0;
      background: linear-gradient(135deg, #1a1a2e, #16213e);
      padding: 16px 20px;
      border-top: 2px solid #f5c542;
      z-index: 9999;
      align-items: center;
      justify-content: space-between;
      flex-wrap: wrap;
      gap: 12px;
    }
    #installBanner.show { display: flex; }
    #installBanner .install-text { display: flex; align-items: center; gap: 12px; }
    #installBanner .install-text i { font-size: 2rem; color: #f5c542; }
    #installBanner .install-text span { font-weight: 600; font-size: 1rem; }
    #installBanner .install-text small { display: block; color: #a0a0b8; font-size: 0.8rem; }
    #installBanner .install-btn {
      background: #f5c542;
      border: none;
      padding: 10px 28px;
      border-radius: 30px;
      font-weight: 700;
      cursor: pointer;
      color: #0e0e12;
    }
    #installBanner .close-banner {
      background: transparent;
      border: none;
      color: #888;
      font-size: 1.5rem;
      cursor: pointer;
      padding: 0 8px;
    }
    
    /* Navbar */
    .navbar {
      display: flex;
      flex-wrap: wrap;
      justify-content: space-between;
      align-items: center;
      padding: 1.2rem 0;
      border-bottom: 1px solid #2a2a33;
      margin-bottom: 2rem;
      gap: 0.8rem;
    }
    .logo {
      font-size: 1.9rem;
      font-weight: 700;
      display: flex;
      align-items: center;
      gap: 10px;
      text-decoration: none;
    }
    .god-fortune-logo {
      display: flex;
      align-items: center;
      justify-content: center;
      width: 44px;
      height: 44px;
      background: radial-gradient(circle at 30% 30%, #f7d875, #c99f2b);
      border-radius: 50%;
      box-shadow: 0 0 0 2px #b8860b;
      font-size: 1.6rem;
      font-weight: 800;
      color: #2d1f04;
    }
    .logo-text {
      background: linear-gradient(135deg, #f0c27f, #f5a623);
      -webkit-background-clip: text;
      background-clip: text;
      color: transparent;
    }
    .nav-right { display: flex; align-items: center; gap: 1.8rem; flex-wrap: wrap; }
    .nav-links { display: flex; gap: 2rem; font-weight: 500; }
    .nav-links a { color: #b0b0c0; text-decoration: none; transition: 0.2s; display: flex; align-items: center; gap: 6px; }
    .nav-links a:hover { color: #f5c542; }
    .nav-links .active { color: #f5c542; border-bottom: 2px solid #f5c542; padding-bottom: 6px; }
    
    .auth-buttons { display: flex; gap: 10px; }
    .btn-google {
      display: flex;
      align-items: center;
      gap: 8px;
      background: #1e1e2a;
      border: 1px solid #3d3d4f;
      color: #e5e5eb;
      padding: 0.4rem 1.2rem;
      border-radius: 40px;
      font-weight: 500;
      cursor: pointer;
      transition: 0.2s;
    }
    .btn-google:hover { background: #2a2a3a; border-color: #f5c542; }
    .btn-google.signup { background: #f5c542; border-color: #f5c542; color: #0e0e12; }
    
    .search-box {
      display: flex;
      align-items: center;
      background: #1e1e26;
      padding: 0.4rem 1rem;
      border-radius: 40px;
      border: 1px solid #33333f;
    }
    .search-box input {
      background: transparent;
      border: none;
      outline: none;
      color: #e5e5eb;
      padding: 0.3rem 0;
      width: 160px;
    }
    
    /* Featured */
    .featured {
      background: linear-gradient(145deg, #1a1a24, #12121a);
      border-radius: 28px;
      padding: 1.8rem 2.2rem;
      margin-bottom: 2.8rem;
      display: flex;
      flex-wrap: wrap;
      align-items: center;
      justify-content: space-between;
      border: 1px solid #2d2d3b;
    }
    .featured-text h2 { font-size: 1.8rem; font-weight: 600; }
    .featured-text h2 i { color: #f5a623; margin-right: 12px; }
    .featured-text p { color: #a5a5b8; max-width: 440px; margin-top: 4px; }
    .featured-tag {
      background: #2a2a36;
      padding: 0.6rem 1.8rem;
      border-radius: 60px;
      font-weight: 500;
      border: 1px solid #3d3d4d;
    }
    .featured-tag i { color: #f5a623; margin-right: 8px; }
    
    /* Cards */
    .section-header {
      display: flex;
      justify-content: space-between;
      align-items: baseline;
      margin: 2.2rem 0 1.2rem;
    }
    .section-header h3 { font-size: 1.5rem; font-weight: 600; }
    .section-header a { color: #8e8ea8; text-decoration: none; }
    .section-header a:hover { color: #f5c542; }
    
    .card-grid {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(160px, 1fr));
      gap: 1.8rem 1.5rem;
    }
    .manga-card {
      background: #17171f;
      border-radius: 22px;
      overflow: hidden;
      transition: transform 0.2s;
      border: 1px solid #262632;
      box-shadow: 0 8px 20px rgba(0,0,0,0.5);
      cursor: pointer;
    }
    .manga-card:hover { transform: translateY(-6px); border-color: #3f3f55; }
    .card-img {
      background: #242431;
      height: 210px;
      display: flex;
      align-items: center;
      justify-content: center;
      color: #6b6b84;
      font-size: 3.2rem;
      position: relative;
    }
    .card-badge {
      position: absolute;
      top: 12px;
      left: 12px;
      background: #1c1c28;
      padding: 0.2rem 0.9rem;
      border-radius: 30px;
      font-size: 0.65rem;
      font-weight: 600;
      color: #c0c0d0;
      border: 1px solid #3a3a4a;
    }
    .card-body { padding: 0.8rem 1rem 1rem; }
    .card-body h4 { font-weight: 600; font-size: 1rem; margin-bottom: 4px; white-space: nowrap; overflow: hidden; text-overflow: ellipsis; }
    .card-body .meta { display: flex; justify-content: space-between; color: #8a8aa0; font-size: 0.75rem; margin-top: 6px; }
    .card-body .rating { color: #f5b342; }
    
    .filter-chips {
      display: flex;
      flex-wrap: wrap;
      gap: 0.6rem 1rem;
      margin: 0.8rem 0 0.2rem;
    }
    .chip {
      background: #1b1b25;
      padding: 0.3rem 1.2rem;
      border-radius: 40px;
      font-size: 0.8rem;
      font-weight: 500;
      color: #b5b5cc;
      border: 1px solid #2f2f3d;
      cursor: pointer;
      transition: 0.15s;
    }
    .chip.active { background: #2d2d40; border-color: #f5b342; color: #f5e3c0; }
    .chip:hover { background: #252535; }
    
    .footer {
      margin-top: 4rem;
      padding-top: 2rem;
      border-top: 1px solid #262632;
      display: flex;
      flex-wrap: wrap;
      justify-content: space-between;
      color: #7c7c94;
      font-size: 0.9rem;
    }
    .footer-links span { margin-right: 2rem; }
    .footer-links i { margin-right: 6px; color: #5f5f78; }
    
    /* Toast */
    #toast {
      position: fixed;
      bottom: 100px;
      left: 50%;
      transform: translateX(-50%);
      background: #1a1a2e;
      padding: 12px 24px;
      border-radius: 30px;
      border: 1px solid #f5c542;
      box-shadow: 0 8px 30px rgba(0,0,0,0.8);
      z-index: 9998;
      opacity: 0;
      transition: opacity 0.3s;
      pointer-events: none;
      display: flex;
      align-items: center;
      gap: 10px;
    }
    #toast.show { opacity: 1; }
    #toast i { color: #f5c542; }
    
    @media (max-width: 820px) {
      .navbar { flex-direction: column; align-items: stretch; }
      .nav-right { flex-direction: column; align-items: stretch; }
      .nav-links { justify-content: space-around; flex-wrap: wrap; }
      .auth-buttons { justify-content: center; flex-wrap: wrap; }
      .search-box { width: 100%; }
      .search-box input { width: 100%; }
      .featured { flex-direction: column; align-items: start; gap: 1rem; }
      .card-grid { grid-template-columns: repeat(auto-fill, minmax(140px, 1fr)); }
    }
    @media (max-width: 480px) {
      body { padding: 0 0.8rem 2rem; }
      .card-img { height: 170px; }
      .god-fortune-logo { width: 38px; height: 38px; font-size: 1.3rem; }
    }
  </style>
</head>
<body>

  <!-- Toast -->
  <div id="toast"><i class="fas fa-info-circle"></i> <span id="toastMessage"></span></div>

  <!-- Install Banner -->
  <div id="installBanner">
    <div class="install-text">
      <i class="fas fa-download"></i>
      <div>
        <span>Install Kagami</span>
        <small>Baca manga & nonton anime offline</small>
      </div>
    </div>
    <div>
      <button class="install-btn" id="installAppBtn"><i class="fas fa-arrow-down"></i> Install</button>
      <button class="close-banner" id="closeBannerBtn">✕</button>
    </div>
  </div>

  <!-- Navbar -->
  <nav class="navbar">
    <a href="#" class="logo">
      <div class="god-fortune-logo"><span>福</span></div>
      <span class="logo-text"><i class="fas fa-book-open"></i> Kagami</span>
    </a>
    <div class="nav-right">
      <div class="nav-links">
        <a href="#" class="active"><i class="fas fa-compass"></i> Explore</a>
        <a href="#"><i class="fas fa-fire"></i> Trending</a>
        <a href="#"><i class="fas fa-clock"></i> Latest</a>
        <a href="#"><i class="fas fa-bookmark"></i> Saved</a>
      </div>
      <div class="auth-buttons">
        <button class="btn-google" id="loginBtn"><i class="fab fa-google"></i> Log in</button>
        <button class="btn-google signup" id="signupBtn"><i class="fab fa-google"></i> Sign up</button>
      </div>
      <div class="search-box">
        <i class="fas fa-search"></i>
        <input type="text" placeholder="Search titles..." id="searchInput">
      </div>
    </div>
  </nav>

  <!-- Featured -->
  <div class="featured">
    <div class="featured-text">
      <h2><i class="fas fa-star"></i> Featured this week</h2>
      <p>Discover the hottest manga, manhwa & manhua — updated daily.</p>
    </div>
    <div class="featured-tag"><i class="fas fa-arrow-right"></i> 12 new chapters</div>
  </div>

  <!-- Filter -->
  <div class="filter-chips">
    <span class="chip active" data-filter="all"><i class="fas fa-th-list"></i> All</span>
    <span class="chip" data-filter="manga"><i class="fas fa-flag"></i> Manga</span>
    <span class="chip" data-filter="manhwa"><i class="fas fa-flag"></i> Manhwa</span>
    <span class="chip" data-filter="manhua"><i class="fas fa-flag"></i> Manhua</span>
    <span class="chip" data-filter="anime"><i class="fas fa-film"></i> Anime</span>
  </div>

  <!-- Sections -->
  <div class="section-header">
    <h3><i class="fas fa-book" style="color: #f5a623;"></i> Manga</h3>
    <a href="#">View all <i class="fas fa-chevron-right"></i></a>
  </div>
  <div class="card-grid" id="mangaGrid"></div>

  <div class="section-header">
    <h3><i class="fas fa-palette" style="color: #6fc3df;"></i> Manhwa</h3>
    <a href="#">View all <i class="fas fa-chevron-right"></i></a>
  </div>
  <div class="card-grid" id="manhwaGrid"></div>

  <div class="section-header">
    <h3><i class="fas fa-landmark" style="color: #e6a8d7;"></i> Manhua</h3>
    <a href="#">View all <i class="fas fa-chevron-right"></i></a>
  </div>
  <div class="card-grid" id="manhuaGrid"></div>

  <div class="section-header" style="margin-top: 3rem; border-top: 1px solid #2a2a33; padding-top: 2rem;">
    <h3><i class="fas fa-film" style="color: #f5a623;"></i> Watch Anime</h3>
    <a href="#">View all <i class="fas fa-chevron-right"></i></a>
  </div>
  <div class="card-grid" id="animeGrid"></div>

  <!-- Footer -->
  <div class="footer">
    <div>© 2026 Kagami · read freely</div>
    <div class="footer-links">
      <span><i class="fas fa-info-circle"></i> About</span>
      <span><i class="fas fa-envelope"></i> Contact</span>
      <span><i class="fas fa-shield-alt"></i> Privacy</span>
    </div>
  </div>

  <script>
    // ====== DATA ======
    const mangaData = [
      { title: "Solo Leveling", type: "Manhwa", rating: 4.9, chapters: 179, color: "#2d2d44" },
      { title: "One Piece", type: "Manga", rating: 4.8, chapters: 1102, color: "#2f3a4a" },
      { title: "Noblesse", type: "Manhwa", rating: 4.7, chapters: 544, color: "#3a2a3a" },
      { title: "Tower of God", type: "Manhwa", rating: 4.6, chapters: 590, color: "#1f3f3f" },
      { title: "Jujutsu Kaisen", type: "Manga", rating: 4.8, chapters: 271, color: "#2d2a40" },
      { title: "Battle Through the Heavens", type: "Manhua", rating: 4.5, chapters: 402, color: "#3a3a2a" },
      { title: "My Hero Academia", type: "Manga", rating: 4.4, chapters: 398, color: "#2f3545" },
      { title: "The Breaker", type: "Manhwa", rating: 4.7, chapters: 200, color: "#2d3d3d" },
      { title: "Martial Peak", type: "Manhua", rating: 4.3, chapters: 2400, color: "#2a3f2a" },
      { title: "Demon Slayer", type: "Manga", rating: 4.9, chapters: 205, color: "#3a2a2a" },
      { title: "Chainsaw Man", type: "Manga", rating: 4.7, chapters: 150, color: "#3f2a2a" },
      { title: "Tales of Demons and Gods", type: "Manhua", rating: 4.6, chapters: 455, color: "#2f3a2f" }
    ];

    const animeData = [
      { title: "Jujutsu Kaisen", episodes: 24, rating: 4.8, color: "#2d2a40" },
      { title: "Demon Slayer", episodes: 44, rating: 4.9, color: "#3a2a2a" },
      { title: "One Piece", episodes: 1100, rating: 4.7, color: "#2f3a4a" },
      { title: "Attack on Titan", episodes: 87, rating: 4.8, color: "#2a2f3f" },
      { title: "Spy x Family", episodes: 25, rating: 4.7, color: "#2d3d3d" }
    ];

    // ====== RENDER ======
    function createCard(item, isAnime = false) {
      const icon = isAnime ? "fa-play-circle" : "fa-book";
      const badge = isAnime ? "Anime" : item.type;
      const metaEp = isAnime ? `${item.episodes} eps` : `${item.chapters} ch`;
      return `
        <div class="manga-card" data-title="${item.title}" data-type="${isAnime ? 'anime' : item.type.toLowerCase()}">
          <div class="card-img" style="background: ${item.color || '#242431'};">
            <i class="fas ${icon}"></i>
            <span class="card-badge">${badge}</span>
          </div>
          <div class="card-body">
            <h4>${item.title}</h4>
            <div class="meta">
              <span><i class="fas ${isAnime ? 'fa-clock' : 'fa-book-open'}"></i> ${metaEp}</span>
              <span class="rating"><i class="fas fa-star"></i> ${item.rating}</span>
            </div>
          </div>
        </div>
      `;
    }

    function renderGrid(id, type) {
      const container = document.getElementById(id);
      if (!container) return;
      let items = mangaData.filter(m => m.type === type);
      items = items.slice(0, 6);
      container.innerHTML = items.map(m => createCard(m)).join('');
    }

    function renderAnime(id) {
      const container = document.getElementById(id);
      if (!container) return;
      let items = animeData.slice(0, 6);
      container.innerHTML = items.map(m => createCard(m, true)).join('');
    }

    // ====== TOAST ======
    function showToast(msg) {
      const toast = document.getElementById('toast');
      document.getElementById('toastMessage').textContent = msg;
      toast.classList.add('show');
      clearTimeout(toast._timeout);
      toast._timeout = setTimeout(() => toast.classList.remove('show'), 3000);
    }

    // ====== INSTALL PWA ======
    let deferredPrompt;
    const installBanner = document.getElementById('installBanner');

    window.addEventListener('beforeinstallprompt', (e) => {
      e.preventDefault();
      deferredPrompt = e;
      installBanner.classList.add('show');
    });

    document.getElementById('installAppBtn').addEventListener('click', async () => {
      if (deferredPrompt) {
        deferredPrompt.prompt();
        const result = await deferredPrompt.userChoice;
        if (result.outcome === 'accepted') {
          showToast('✅ Kagami berhasil diinstall!');
          installBanner.classList.remove('show');
        }
        deferredPrompt = null;
      }
    });

    document.getElementById('closeBannerBtn').addEventListener('click', () => {
      installBanner.classList.remove('show');
    });

    // ====== SERVICE WORKER ======
    if ('serviceWorker' in navigator) {
      navigator.serviceWorker.register('/service-worker.js')
        .then(() => console.log('✅ Service Worker OK'))
        .catch(() => console.log('❌ Service Worker failed'));
    }

    // ====== EVENT ======
    document.querySelectorAll('.chip').forEach(chip => {
      chip.addEventListener('click', function() {
        document.querySelectorAll('.chip').forEach(c => c.classList.remove('active'));
        this.classList.add('active');
        const filter = this.dataset.filter;
        document.querySelectorAll('.manga-card').forEach(card => {
          card.style.display = (filter === 'all' || card.dataset.type === filter) ? 'block' : 'none';
        });
        showToast(`Menampilkan: ${filter}`);
      });
    });

    document.querySelectorAll('.manga-card, .card-grid').forEach(el => {
      el.addEventListener('click', (e) => {
        const card = e.target.closest('.manga-card');
        if (card) showToast(`📖 Membuka: ${card.dataset.title}`);
      });
    });

    document.getElementById('loginBtn').addEventListener('click', () => showToast('🔐 Login dengan Google'));
    document.getElementById('signupBtn').addEventListener('click', () => showToast('📝 Daftar dengan Google'));

    document.getElementById('searchInput').addEventListener('keydown', function(e) {
      if (e.key === 'Enter' && this.value.trim()) {
        showToast(`🔍 Mencari: "${this.value}"`);
      }
    });

    // ====== INIT ======
    renderGrid('mangaGrid', 'Manga');
    renderGrid('manhwaGrid', 'Manhwa');
    renderGrid('manhuaGrid', 'Manhua');
    renderAnime('animeGrid');

    setTimeout(() => showToast('📚 Selamat datang di Kagami!'), 1000);
  </script>
</body>
</html>